"""MCA blender MCP server package.

The runnable server lives in :mod:`mca_mcp.blender.server`.  Launch it with::

    python -m mca_mcp.blender.server

or via the installer CLI (``mca-mcp install blender``), which deploys the server
and registers it with Claude Code.
"""
