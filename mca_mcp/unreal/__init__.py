"""MCA unreal MCP server package.

The runnable server lives in :mod:`mca_mcp.unreal.server`.  Launch it with::

    python -m mca_mcp.unreal.server

or via the installer CLI (``mca-mcp install unreal``), which deploys the server
and registers it with Claude Code.
"""
